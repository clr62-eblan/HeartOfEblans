generic_nationalisation_cat = {
	generic_nationalisation_decision = {
		priority = 10

		icon = generic_construction

		ai_will_do = {
			base = 20
		}

		state_target = yes
        #days_mission_timeout = 75

		#targets = { core_states }

		target_trigger = {
			FROM = {
				is_fully_controlled_by = ROOT
                NOT = { is_core_of = ROOT }
			}
		}

		target_root_trigger = {
			has_country_flag = generic_nationalisation_flag
			has_civil_war = no
		}

		allowed = {
			has_country_flag = generic_nationalisation_flag
		}

		available = {
			FROM = {
				is_fully_controlled_by = ROOT
			}
		}

		visible = {
			FROM = {
				is_fully_controlled_by = ROOT
			}
		}

		cancel_if_not_visible = no

		cost = 350

        complete_effect = {
            FROM = {
                add_core_of = ROOT
            }
        }

		#timeout_effect = { 
        #    FROM = {
        #        add_core_of = ROOT
        #    }
        #}
	}
}
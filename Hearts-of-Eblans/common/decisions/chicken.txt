CHB_peace_deals_category = {
	start_peace_conferece = {
		icon = eng_trade_unions_support
		cost = 100
	
		allowed = {
			original_tag = CHB
		}
		
		complete_effect = {
			country_event = { id = chicken.15 }
			set_country_flag = peace_started
		}
	}
}

#####################################
### БАТАЛЬОНЫ СМАРТИКОВ ###
#####################################

CHB_smartiki_category = {

	# Батальон "Смартик-Альфа"
	CHB_spawn_smartiki_alpha = {
		icon = CHB_simplesmart
		cost = 50

		fire_only_once = no

		days_re_enable = 200

		allowed = {
			original_tag = CHB
		}

		available = {
			has_war = yes
			num_of_military_factories > 5
		}

		visible = {
			original_tag = CHB
		}

		days_remove = 20

		complete_effect = {
			custom_effect_tooltip = CHB_smartiki_training_tt
			add_manpower = -1000
		}

		remove_effect = {
			set_country_flag = CHB_SMARIK_SPAWNED
			custom_effect_tooltip = CHB_smartiki_spawned_tt
			random_owned_controlled_state = {
				limit = { 
					ROOT = { has_full_control_of_state = PREV }
					is_core_of = ROOT
				}
				create_unit = {
					division = "name = \"1-й бат. Смартик-Альфа\" division_template = \"Smartiki Battalion\" start_experience_factor = 0.6 start_equipment_factor = 1.0"
					owner = CHB
				}
			}
		}
	}

	# Батальон "Смартик-Браво"
	CHB_spawn_smartiki_bravo = {
		icon = CHB_simplesmart
		cost = 50

		fire_only_once = no

		days_re_enable = 200

		allowed = {
			original_tag = CHB
		}

		available = {
			has_war = yes
			num_of_military_factories > 5
		}

		visible = {
			original_tag = CHB
		}

		days_remove = 20

		complete_effect = {
			custom_effect_tooltip = CHB_smartiki_training_tt
			add_manpower = -1000
		}

		remove_effect = {
			set_country_flag = CHB_SMARIK_SPAWNED
			custom_effect_tooltip = CHB_smartiki_spawned_tt
			random_owned_controlled_state = {
				limit = { 
					ROOT = { has_full_control_of_state = PREV }
					is_core_of = ROOT
				}
				create_unit = {
					division = "name = \"2-й бат. Смартик-Браво\" division_template = \"Smartiki Battalion\" start_experience_factor = 0.6 start_equipment_factor = 1.0"
					owner = CHB
				}
			}
		}
	}

	# Батальон "Смартик-Чарли"
	CHB_spawn_smartiki_charlie = {
		icon = CHB_simplesmart
		cost = 50

		fire_only_once = no

		days_re_enable = 200

		allowed = {
			original_tag = CHB
		}

		available = {
			has_war = yes
			num_of_military_factories > 8
		}

		visible = {
			original_tag = CHB
		}

		days_remove = 20

		complete_effect = {
			custom_effect_tooltip = CHB_smartiki_training_tt
			add_manpower = -1000
		}

		remove_effect = {
			set_country_flag = CHB_SMARIK_SPAWNED
			custom_effect_tooltip = CHB_smartiki_spawned_tt
			random_owned_controlled_state = {
				limit = { 
					ROOT = { has_full_control_of_state = PREV }
					is_core_of = ROOT
				}
				create_unit = {
					division = "name = \"3-й бат. Смартик-Чарли\" division_template = \"Smartiki Battalion\" start_experience_factor = 0.6 start_equipment_factor = 1.0"
					owner = CHB
				}
			}
		}
	}

	# Батальон "Смартик-Дельта"
	CHB_spawn_smartiki_delta = {
		icon = CHB_simplesmart
		cost = 50

		fire_only_once = no

		days_re_enable = 200

		allowed = {
			original_tag = CHB
		}

		available = {
			has_war = yes
			num_of_military_factories > 10
		}

		visible = {
			original_tag = CHB
		}

		days_remove = 20

		complete_effect = {
			custom_effect_tooltip = CHB_smartiki_training_tt
			add_manpower = -1000
		}

		remove_effect = {
			set_country_flag = CHB_SMARIK_SPAWNED
			custom_effect_tooltip = CHB_smartiki_spawned_tt
			random_owned_controlled_state = {
				limit = { 
					ROOT = { has_full_control_of_state = PREV }
					is_core_of = ROOT
				}
				create_unit = {
					division = "name = \"4-й бат. Смартик-Дельта\" division_template = \"Smartiki Battalion\" start_experience_factor = 0.6 start_equipment_factor = 1.0"
					owner = CHB
				}
			}
		}
	}

	CHB_spawn_smartiki_echo = {
		icon = CHB_elitesmart
		cost = 100

		fire_only_once = no

		days_re_enable = 200

		allowed = {
			original_tag = CHB
		}

		available = {
			has_war = yes
			num_of_military_factories > 15
		}

		visible = {
			original_tag = CHB
		}

		days_remove = 20

		complete_effect = {
			custom_effect_tooltip = CHB_smartiki_elite_training_tt
			add_manpower = -1500
		}

		remove_effect = {
			set_country_flag = CHB_SMARIK_SPAWNED
			custom_effect_tooltip = CHB_smartiki_elite_spawned_tt
			random_owned_controlled_state = {
				limit = { 
					ROOT = { has_full_control_of_state = PREV }
					is_core_of = ROOT
				}
				create_unit = {
					division = "name = \"5-й бат. Смартик-Эхо\" division_template = \"Smartiki Elite\" start_experience_factor = 0.8 start_equipment_factor = 1.0"
					owner = CHB
				}
			}
		}
	}
	CHB_spawn_smartiki_foxtrot = {
		icon = CHB_elitesmart
		cost = 100

		fire_only_once = no

		days_re_enable = 200

		allowed = {
			original_tag = CHB
		}

		available = {
			has_war = yes
			num_of_military_factories > 15
		}

		visible = {
			original_tag = CHB
		}

		days_remove = 20

		complete_effect = {
			custom_effect_tooltip = CHB_smartiki_elite_training_tt
			add_manpower = -1500
		}

		remove_effect = {
			set_country_flag = CHB_SMARIK_SPAWNED
			custom_effect_tooltip = CHB_smartiki_elite_spawned_tt
			random_owned_controlled_state = {
				limit = { 
					ROOT = { has_full_control_of_state = PREV }
					is_core_of = ROOT
				}
				create_unit = {
					division = "name = \"6-й бат. Смартик-Фокстрот\" division_template = \"Smartiki Elite\" start_experience_factor = 0.8 start_equipment_factor = 1.0"
					owner = CHB
				}
			}
		}
	}

	CHB_spawn_smartiki_kilo = {
		icon = CHB_elitesmart
		cost = 100

		fire_only_once = no

		days_re_enable = 200

		allowed = {
			original_tag = CHB
		}

		available = {
			has_war = yes
			num_of_military_factories > 15
		}

		visible = {
			original_tag = CHB
		}

		days_remove = 20

		complete_effect = {
			custom_effect_tooltip = CHB_smartiki_elite_training_tt
			add_manpower = -1500
		}

		remove_effect = {
			set_country_flag = CHB_SMARIK_SPAWNED
			custom_effect_tooltip = CHB_smartiki_elite_spawned_tt
			random_owned_controlled_state = {
				limit = { 
					ROOT = { has_full_control_of_state = PREV }
					is_core_of = ROOT
				}
				create_unit = {
					division = "name = \"7-й бат. Смартик-Кило\" division_template = \"Smartiki Elite\" start_experience_factor = 0.8 start_equipment_factor = 1.0"
					owner = CHB
				}
			}
		}
	}
}

CHB_bekonian_subjects = {
	CHB_establish_subject_decision = {

		icon = GFX_decision_eng_trade_unions_support

		priority = 10

		allowed = {
			original_tag = CHB
		}

		visible = {

		}

		available = {

		}

		cost = 25

		days_remove = 20

		ai_will_do = {
			base = 3
		}

		fire_only_once = yes

		target_array = ROOT.subjects

		target_trigger = {
			FROM = {
				has_government = communism
				any_home_area_neighbor_country = {
					original_tag = CHB
				}

			}
		}

		complete_effect = {

		}

		remove_effect = {

			FROM = {
				country_event = {
					id = chicken.40
					hours = 3
				}
			}

			event_option_tooltip = wuw_GER_diplomacy.71.a
		}
	}

	CHB_integrate_subject_decision = {
		icon = GFX_decision_hol_draw_up_staff_plans

		priority = 11

		allowed = {
			original_tag = CHB
		}

		visible = {
			has_completed_focus = chb_integrate
		}

		available = {
			NOT = { 
				has_country_flag = CHB_currently_integrating_subject_flag
			}
		}

		cost = 50

		days_remove = 15

		ai_will_do = {
			base = 5
		}

		fire_only_once = yes

		target_array = ROOT.subjects

		target_trigger = {
			FROM = {
				has_autonomy_state = autonomy_bekonianstate
			}
		}

		complete_effect = {
			set_country_flag = CHB_currently_integrating_subject_flag
		}

		remove_effect = {

			IF = { #We're using a flag here instead so that you don't accidentally remove a potential cosmetic tag you gain AFTER taking this decision for the first time
				limit = {
					NOT = {
						has_country_flag = CHB_integrate_subjects_cosmetic_tag_flag
					}
				}
				set_country_flag = CHB_integrate_subjects_cosmetic_tag_flag

				set_cosmetic_tag = CHB_socialism_union
			}
			set_country_flag = CHB_integrate_subjects_cosmetic_tag_flag

			clr_country_flag = CHB_currently_integrating_subject_flag

			FROM = {
				country_event = {
					id = chicken.41
					hours = 12
				}
			}
		}
	}
}